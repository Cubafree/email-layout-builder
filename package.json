{
  "name": "email-layout-builder",
  "version": "1.0.0",
  "description": "Email layout processing UI for n8n pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
